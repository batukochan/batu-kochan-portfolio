export interface ShowcaseProps {
    avatar?: string; // Profil fotoğrafı URL'si
    name?: string; // Kullanıcı adı
    title?: string; // Başlık (örneğin, "Full-Stack Developer")
    description?: string; // Kullanıcının kısa açıklaması
    location?: string; // Kullanıcının lokasyonu
  }
  